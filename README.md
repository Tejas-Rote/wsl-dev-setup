# Dev Environment

Personal development environment setup.

## Branches
- `ml`  – Machine learning setup (Conda, GPU, TF, PyTorch)
- `hpc` – HPC development setup (SLURM, tooling)
- `wsl` – WSL + GPU setup (planned)
