# HPC Project Template

This is a clean template for HPC projects.

⚠️ **Do NOT build inside this directory.**

## Usage

Create a project:
```bash
cp -r templates/template-hpc projects/<project_name>
